packages:
  - "packages/*"
  - "apps/*"

# esbuild ships a prebuilt binary and needs its postinstall to run; vitest
# depends on it. Nothing else in this repo builds on install.
allowBuilds:
  esbuild: true
